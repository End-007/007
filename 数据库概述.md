### 一、数据库设计方案 (MySQL)

数据库设计分为 **基础用户模块**、**教学管理模块**、**科研与竞赛模块**、**人事与档案模块** 四大板块。

#### 1. 基础用户模块

用于系统登录、权限控制及基础身份关联。

- **`sys_user` (用户表)**

  - `id` (BIGINT, PK): 主键
  - `username` (VARCHAR(50)): 用户名/工号/学号
  - `password` (VARCHAR(100)): 加密密码
  - `role` (ENUM: 'ADMIN', 'TEACHER', 'STUDENT'): 角色
  - `status` (TINYINT): 状态 (1 正常, 0 禁用)
  - `created_at` (DATETIME): 创建时间

- **`base_student` (学生扩展表)**

  - `id` (BIGINT, PK)
  - `user_id` (BIGINT, FK): 关联用户表
  - `student_no` (VARCHAR(20)): 学号
  - `name` (VARCHAR(50)): 姓名
  - `class_name` (VARCHAR(50)): 班级
  - `department` (VARCHAR(50)): 系别
  - `grade` (VARCHAR(10)): 年级

- **`base_teacher` (教师扩展表)**
  - `id` (BIGINT, PK)
  - `user_id` (BIGINT, FK): 关联用户表
  - `teacher_no` (VARCHAR(20)): 工号
  - `name` (VARCHAR(50)): 姓名
  - `title` (VARCHAR(50)): 职称
  - `department` (VARCHAR(50)): 所属系

#### 2. 教学管理模块

- **`teach_course` (课程表)**
  - `id` (BIGINT, PK)
  - `course_no` (VARCHAR(20)): 课程编号
  - `course_name` (VARCHAR(100)): 课程名称
  - `teacher_id` (BIGINT, FK): 任课教师 ID
  - `semester` (VARCHAR(20)): 学期 (如: 2023-2024-1)
  - `credits` (DECIMAL(3,1)): 学分

#### 3. 科研与竞赛模块 (基于调研记录细化)

- **`res_competition` (竞赛获奖表)**

  - `id` (BIGINT, PK)
  - `participant_id` (BIGINT): 参与者 ID (学生或教师)
  - `participant_type` (ENUM: 'STUDENT', 'TEACHER'): 参与者类型
  - `comp_name` (VARCHAR(100)): 竞赛名称 (如: 蓝桥杯)
  - `level` (VARCHAR(20)): 级别 (国赛/省赛)
  - `award_grade` (VARCHAR(20)): 奖项等级 (一等奖/二等奖等)
  - `data_source` (VARCHAR(20)): 数据来源 (学院统计/学校下发)
  - `award_date` (DATE): 获奖日期
  - `remark` (VARCHAR(255)): 备注 (调研要求所有论文/奖项增加备注列)

- **`res_innovation` (大创项目表)**

  - `id` (BIGINT, PK)
  - `project_no` (VARCHAR(30)): 项目编号
  - `project_name` (VARCHAR(100)): 项目名称
  - `leader_id` (BIGINT): 负责人 ID
  - `level` (VARCHAR(20)): 级别 (国家级/省级/校级)
  - `status` (VARCHAR(20)): 状态 (立项/进行中/结题)
  - `funding` (DECIMAL(10,2)): 经费
  - `start_date` (DATE): 开始日期
  - `end_date` (DATE): 结束日期

- **`res_paper` (论文发表表)**

  - `id` (BIGINT, PK)
  - `author_id` (BIGINT): 作者 ID
  - `title` (VARCHAR(200)): 论文标题
  - `journal_name` (VARCHAR(100)): 期刊名称
  - `publish_date` (DATE): 发表时间
  - `index_type` (VARCHAR(20)): 收录类型 (SCI/EI/CORE 等)
  - `remark` (VARCHAR(255)): **备注** (调研特别强调)

- **`res_project` (科研项目表)**

  - `id` (BIGINT, PK)
  - `project_name` (VARCHAR(100)): 项目名称
  - `principal_id` (BIGINT): 负责人 ID
  - `source` (VARCHAR(50)): 来源 (横向/纵向)
  - `amount` (DECIMAL(12,2)): 项目金额
  - `start_date` (DATE): 起止时间
  - `end_date` (DATE)

- **`res_award` (教研/科研奖励表)**
  - `id` (BIGINT, PK)
  - `recipient_id` (BIGINT): 获得者 ID
  - `award_name` (VARCHAR(100)): 奖励名称
  - `award_type` (ENUM: 'TEACHING', 'RESEARCH'): 类型 (教研/科研)
  - `level` (VARCHAR(20)): 级别
  - `award_date` (DATE)

#### 4. 人事与档案模块 (深度结合调研记录)

- **`hr_personnel` (人事详细信息表)**

  - `id` (BIGINT, PK)
  - `teacher_id` (BIGINT, FK): 关联教师表
  - `hire_date` (DATE): 入职日期
  - `first_contract_end` (DATE): **首聘期日期** (新入职必填)
  - `regularization_date` (DATE): **转正日期** (新入职必填)
  - `intro_level` (VARCHAR(50)): **引进层次**
  - `special_level` (VARCHAR(50)): **特聘层次**
  - `special_contract_date` (DATE): 特聘合同日期
  - `special_contract_file` (VARCHAR(255)): 特聘合同上传路径
  - `religion` (VARCHAR(50)): 宗教信仰
  - `research_field` (VARCHAR(100)): 研究领域

- **`hr_education_exp` (学习经历表 - 一对多)**

  - `id` (BIGINT, PK)
  - `teacher_id` (BIGINT, FK)
  - `school_name` (VARCHAR(100)): 院校名称
  - `degree` (VARCHAR(20)): 学位 (大专/本科/硕士/博士)
  - `major` (VARCHAR(50)): 专业
  - `start_date` (DATE): 起始时间
  - `end_date` (DATE): 结束时间
  - `grad_cert_path` (VARCHAR(255)): 毕业证书扫描件路径
  - `deg_cert_path` (VARCHAR(255)): 学位证书扫描件路径

- **`hr_work_exp` (工作/兼职经历表)**

  - `id` (BIGINT, PK)
  - `teacher_id` (BIGINT, FK)
  - `exp_type` (ENUM: 'SOCIAL', 'PART_TIME'): 类型 (社会兼职/个人兼职)
  - `company_name` (VARCHAR(100)): 单位名称/创办公司名称
  - `position` (VARCHAR(50)): 职位
  - `is_legal_rep` (TINYINT): 是否法人 (创业情况用)
  - `reg_date` (DATE): 成立日期 (创业情况用)
  - `start_date` (DATE): 开始时间
  - `end_date` (DATE): 结束时间

- **`hr_family` (家庭成员/子女信息表 - 一对多，用于妇幼服务)**

  - `id` (BIGINT, PK)
  - `teacher_id` (BIGINT, FK)
  - `relation` (VARCHAR(20)): 关系 (子女/配偶)
  - `name` (VARCHAR(50)): 姓名
  - `birth_date` (DATE): **出生日期** (调研强调需详细)
  - `school_info` (VARCHAR(100)): 上学情况/幼儿园信息
  - `fee_status` (VARCHAR(50)): 费用相关情况

- **`hr_assessment` (年度考核结果表)**

  - `id` (BIGINT, PK)
  - `teacher_id` (BIGINT, FK)
  - `year` (INT): 考核年份
  - `result` (VARCHAR(20)): 考核结果 (优秀/合格/不合格)
  - `type` (VARCHAR(20)): 考核类型 (事业单位考核)

- **`sys_document` (学院文件制度表)**
  - `id` (BIGINT, PK)
  - `doc_number` (VARCHAR(50)): 文号 (如: 院字[2023]01 号)
  - `doc_name` (VARCHAR(100)): 文件名称
  - `doc_type` (VARCHAR(20)): 制度类型 (党字/院字/学字)
  - `issue_date` (DATE): 出台日期
  - `status` (VARCHAR(20)): 废改立情况 (有效/废止/修订)
  - `related_doc` (VARCHAR(255)): 关联文件
  - `interpreter` (VARCHAR(50)): 解释权部门/人员

---

### 二、核心功能表单设计

根据调研记录和概要设计，前端页面应包含以下关键表单：

#### 1. 教师信息采集/更新表单 (最复杂表单)

此表单用于新入职教师登记或现有教师信息完善，对应 `hr_personnel`, `hr_education_exp`, `hr_family` 等表。

- **基本信息区**:
  - 姓名、工号、所属系、职称
  - [输入框] 引进层次、首聘期截止日期、转正日期
  - [输入框] 特聘层次、特聘合同日期
  - [文件上传] 特聘合同扫描件
  - [输入框] 研究领域、宗教信仰
- **学习经历区 (动态列表，可添加多行)**:
  - 时间段 (起 - 止)、毕业院校、专业、学历/学位
  - [文件上传] 毕业证扫描件、学位证扫描件
- **创业与兼职区 (动态列表)**:
  - 类型选择 (社会兼职 / 个人创业)
  - 若选创业：公司名称、法人勾选、成立日期
  - 若选兼职：单位名称、职务、时间段
- **家庭子女区 (动态列表，用于精准服务)**:
  - 子女姓名、出生日期 (精确到日)、目前就读学校/幼儿园、相关费用说明
- **附件区**:
  - 其他荣誉证书上传

#### 2. 科研成果录入表单

供教师自行申报或管理员导入，对应 `res_` 系列表。

- **类型选择**: 竞赛获奖 / 大创项目 / 论文 / 科研项目 / 教研奖励
- **通用字段**: 项目名称/论文标题/竞赛名称、级别、时间、备注 (**必填**)
- **特有字段**:
  - _竞赛_: 获奖等级、数据来源 (学院统计/学校下发)
  - _论文_: 期刊名、收录类型 (SCI/EI)
  - _项目_: 经费金额、起止时间、项目来源
  - _大创_: 项目状态、成员列表

#### 3. 数据导入表单 (管理员专用)

对应概要设计中的“数据导入模块”。

- **导入类型选择**: 下拉框 (学生信息、教师信息、竞赛数据、科研数据)
- **模板下载**: 提供对应类型的 Excel 模板下载链接 (特别是区分“学院统计格式”和“学校下发格式”)
- **文件上传**: 支持 .xlsx, .xls
- **数据预览与校验**: 上传后展示前 10 行数据，高亮显示格式错误行
- **确认导入按钮**

#### 4. 学院文件管理制度表单

对应 `sys_document` 表。

- **文号**: (例: 院字〔2024〕12 号)
- **文件名称**:
- **制度类型**: (下拉: 党字/院字/学字)
- **出台日期**: 日期选择器
- **废改立状态**: (下拉: 现行有效/已废止/修订中)
- **关联文件**: (可链接到其他文件 ID)
- **解释权归属**: (部门或具体人员姓名)
